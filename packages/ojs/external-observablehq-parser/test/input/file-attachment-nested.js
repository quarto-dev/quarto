viewof slider = {
  while (count > 0) {
    let b, c;
    const a = FileAttachment("a");
    if (condition) {
      b = defaultFile ? defaultFileValue : FileAttachment('b');
      c = FileAttachment(`c`);
    }
  }
  return input(a, b, c);
}
